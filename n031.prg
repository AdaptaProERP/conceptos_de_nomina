FUNCTION N031(uPar1,uPar2,uPar3,uPar4,uPar5)
  LOCAL nResult:=0
  IF CONDICION=[A].AND.oNm:cOtraNom=[PR]
     lVariac:=.T.
     RETURN VARIAC
  ENDIF
RETURN nResult
// EOF// EOF
/*
Almacena el monto que será descontado a través del concepto "D030" en el proceso de 
nómina Semanal, Quincenal y Mensual, otra o liquidación hasta lograr el pago total del  
préstamo. Si se requiere personalizar los montos, fechas y/o frecuencia de descuento 
debe solicitarlo a través del formulario de definición de préstamo desde la opción 
"Consulta" de la ficha del trabajador.

/*