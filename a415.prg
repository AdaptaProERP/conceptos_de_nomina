FUNCTION A415(uPar1,uPar2,uPar3,uPar4,uPar5)
  LOCAL nResult:=0
  IF CONDICION=[A] .AND. oNm:cOtraNom=[LI].AND.DESTINO_PR<>[B]                        .AND.FECHA_ING<oNm:dHasta
     lVariac:=.T.
     nResult:=VARIAC
   ENDIF
RETURN nResult
// EOF
