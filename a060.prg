FUNCTION A060(uPar1,uPar2,uPar3,uPar4,uPar5)
  LOCAL nResult:=0

IF  (CONDICION=[A] .OR. CONDICION=[V]) .AND. CONDICION$"AL"  .AND. oNm:cOtraNom="BP"  .AND. FECHA_ING<oNm:dHasta
//       nFactor1:= SALARIO
       lVariac:=.T.
       nResult:=VARIAC
ENDIF
RETURN nResult
// EOF
