FUNCTION H401(uPar1,uPar2,uPar3,uPar4,uPar5)
  LOCAL nResult:=0
 /*
  IF CONDICION=[A] .AND. MESES(FECHA_ING,oNm:dHasta)>3 .AND. FINMES() .AND.        oNm:dHasta>=CTOD("30/06/97")
     nFactor1:=CREAVAR("MAXFCH",MAX(CTOD("18/06/97"),FECHA_ING))
     nFactor2:=INT(DIV(MESES(MAXFCH,oNm:dHasta),12))
     nFactor3:=2*MIN(IIF(nFactor2>1,nFactor2-1,0),30)
     nFactor4:=DIV(60+nFactor3,12)
     nResult:=nFactor4*FUNCION(3)*DIV(CNS(052),360)
  ELSE
      //
  ENDIF
 */
RETURN nResult
// EOF
